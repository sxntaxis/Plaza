# Demo Research

Active implementation guidance lives in `demo/ops/` and active contracts live in `demo/specs/`.

Source audits preserved for traceability live in `demo/research/source_audits/`.

Historical process reports and pre-consolidation planning artifacts are not active contracts. They are recoverable from Git history unless explicitly preserved in `demo/research/history/`.
